{% include 'base.html.twig' %}

{% block body %}
    <h1>Info page</h1>
    <p>Person first name: {{ person.firstName }} last name: {{ person.lastName }}</p>
    <p>Position: {{ info }}</p>
    <a href="/">Welcome</a>

{% endblock %}